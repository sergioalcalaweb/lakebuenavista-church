export const { API_URL } = process.env;
export const { RESEND_API_KEY } = process.env;
export const { EMAIL_FROM } = process.env;
export const { EMAIL_TO } = process.env;
export const { NODE_ENV } = process.env;